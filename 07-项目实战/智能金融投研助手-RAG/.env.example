# 智能金融投研助手 - 环境变量配置

# LLM API 配置（查询改写和答案生成需要）
# 支持 OpenAI 兼容 API（如 DeepSeek、ChatAnywhere、本地 Ollama 等）
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# 不配置 API key 也可以运行索引构建和检索，只有答案生成会降级为展示原始检索结果。
