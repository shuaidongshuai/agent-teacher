from __future__ import annotations

SYSTEM_PROMPT = """你是一个有记忆能力的智能助理。你能够记住用户在之前对话中告诉你的信息。

## 你的记忆

### 长期记忆（从之前的对话中检索到的相关记忆）
{long_term_memories}

### 对话摘要（之前对话的压缩摘要）
{conversation_summary}

### 工作记忆（当前任务的关键状态）
{working_memory}

## 行为准则

1. 如果用户问你之前聊过的信息，先参考记忆再回答。
2. 如果记忆中有相关信息，自然引用，不要机械地说“根据我的记忆”。
3. 如果记忆中没有相关信息，直接说明不知道，不要编造。
4. 在对话中注意收集用户稳定且长期有效的信息，例如身份、偏好、习惯、背景和长期目标。
"""


COMPRESS_PROMPT = """请将以下对话历史压缩为一段简洁摘要。保留关键信息（用户身份、偏好、讨论主题、结论和待办），去除闲聊和重复内容。

## 之前的摘要
{previous_summary}

## 需要压缩的对话
{messages}

## 要求

输出一段 200 字以内的摘要，使用第三人称描述用户，例如“用户提到……”“用户偏好……”。直接输出摘要正文，不要加前缀。
"""


EXTRACT_MEMORIES_PROMPT = """请从以下对话中提取值得长期记住的信息。

## 已有长期记忆
{existing_memories}

## 本轮对话
{messages}

## 提取规则

1. 只提取稳定、长期有效、对后续对话有帮助的信息。
2. 不要提取闲聊、临时情绪、一次性问题、泛泛表述。
3. 如果信息与“已有长期记忆”重复，或只是换一种说法表达同一事实，不要再次输出。
4. 如果两条候选记忆语义接近，请合并成一条更规范、更完整的表达。
5. 优先使用第三人称、陈述句、简洁表达，例如“用户是一名数学教师”。
6. 最多输出 3 条记忆；如果没有新增记忆，返回空列表。

## 输出格式

请严格按 JSON 输出：

```json
{{
  "memories": [
    {{"content": "记忆内容", "category": "分类"}}
  ]
}}
```

`category` 可选值：
- `personal_info`：个人身份、背景、职业、角色
- `preference`：稳定偏好
- `fact`：长期有效的客观事实
- `task`：跨轮持续的重要任务
- `other`：其他

如果没有值得新增的长期记忆，返回：

```json
{{"memories": []}}
```
"""


UPDATE_WORKING_MEMORY_PROMPT = """根据当前对话内容，更新工作记忆。工作记忆用于追踪当前任务的关键状态。

## 当前工作记忆
{current_working_memory}

## 最新对话
用户: {user_message}
助手: {assistant_message}

## 输出格式

```json
{{
  "current_goal": "当前主要目标（一句话）",
  "key_facts": ["关键事实1", "关键事实2"],
  "pending_questions": ["待解决问题1"],
  "reasoning_steps": ["当前推理步骤1"]
}}
```

如果当前没有明确任务目标，`current_goal` 填 `"自由对话"`。
"""
